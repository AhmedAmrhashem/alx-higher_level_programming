#!/usr/bin/python3
def uppercase(str):
    i = len(str)
    for j in range(0,len+1):
       to_upper = ord(str[j]) - 32
       return_to_string = str(to_upper)
       new_string = new_string + return_to_string

    print(f"{new_string}")
